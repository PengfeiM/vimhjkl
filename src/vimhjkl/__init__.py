"""vimhjkl — a terminal vim-technique trainer that drills advanced Vim moves.

Picks up where ``vimtutor`` stops: dot-command, operator+motion grammar, text
objects, registers, macros, ex commands (:g / :normal / ranges), and advanced
search & replace.  Practice happens inside *real* vim/nvim; vimhjkl only sets
up the buffer, launches the editor, and grades the result.
"""

__version__ = "0.1.0"
